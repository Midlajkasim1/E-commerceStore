<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style/adminLogin.css">
    <title>Login Page</title>
    <style>
        /* General Styles */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-color: #121317;
}

/* Container Styling */
.container {
    display: flex;
    align-items: center; /* Centers items vertically */
    justify-content: space-between; /* Ensures space between image and form */
    background-color: #ffffff;
    width: 80%;
    max-width: 1200px;
    padding: 20px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    border-radius: 8px;
    gap: 20px; /* Adds space between image and form */
}

/* Image Styling */
.container img {
    width: 45%;
    border-radius: 8px;
    object-fit: cover;
}

/* Login Box Styling */
.login-box {
    width: 50%;
    padding: 20px;
}

.login-title {
    font-size: 2rem;
    color: #333;
    margin-bottom: 20px;
}

.input-group {
    margin-bottom: 20px;
}

.label {
    display: block;
    font-size: 1rem;
    color: #555;
    margin-bottom: 5px;
}

.input {
    width: 100%;
    padding: 15px; /* Increased padding for better touch support */
    font-size: 1.2rem; /* Increased font size for better readability */
    border: 1px solid #ddd;
    border-radius: 6px; /* Slightly rounded edges for modern look */
    outline: none;
    box-sizing: border-box; /* Ensures consistent sizing */
}

.input:focus {
    border-color: #7e3af2;
    box-shadow: 0 0 4px rgba(126, 58, 242, 0.3); /* Added subtle focus shadow */
}

.login-button {
    width: 100%;
    padding: 15px; /* Increased padding for a larger button */
    font-size: 1.2rem; /* Increased font size */
    color: #fff;
    background-color: #7e3af2;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    transition: background-color 0.3s ease; /* Smooth hover effect */
}

.login-button:hover {
    background-color: #6a2fcc;
}

/* Responsive Layout */
@media (max-width: 768px) {
    .container {
        flex-direction: column; /* Stack items vertically */
        align-items: center;
    }

    .container img {
        width: 100%;
        margin-bottom: 20px; /* Adds space between image and form */
    }

    .login-box {
        width: 100%;
    }
}


/*  */
    </style>
</head>
<body>
    <div class="container">
        <img src="../images/store-logo.png" alt="logo">
        <div class="login-box">
            <h1 class="login-title">Login</h1>
            <form method="POST" action="/admin/login">
                <div class="input-group">
                    <label for="email"  class="label">Email</label>
                    <input type="email" id="email" name="email" class="input" placeholder="Enter your email">
                </div>
                <div class="input-group">
                    <label for="password" class="label">Password</label>
                    <input type="password"  id="password" name="password" class="input" placeholder="Enter your password">
                </div>
                <button type="submit" class="login-button">Log In</button>
            </form>
        </div>
    </div>
</body>
</html>
<!--  -->